services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: mergesafe
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-replace-with-strong-password}
    ports:
      - "5433:5432"
  redis:
    image: redis:7
    ports:
      - "6377:6379"
